reload_integration:
  name: Reload Integration
  description: Reload the Lionel Train Controller integration for better reconnection.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID to reload (optional - uses current integration if not provided).
      required: false
      selector:
        text:
